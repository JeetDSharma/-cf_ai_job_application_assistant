{
  "name": "ai-job-assistant",
  "version": "1.0.0",
  "description": "AI-powered job application assistant built on Cloudflare",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "start:frontend": "cd frontend && npm run dev",
    "build:frontend": "cd frontend && npm run build",
    "preview": "wrangler dev --remote",
    "tail": "wrangler tail",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "typescript": "^5.6.3",
    "vitest": "2.1.5",
    "wrangler": "^3.95.0"
  },
  "dependencies": {
    "hono": "^4.6.14"
  }
}
